name: Report

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"  # weekly Monday 6am UTC
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Build release
        run: cargo build --release

      - name: Run tests
        run: cargo test

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Benchmarks are slow — only run on schedule and workflow_dispatch, not on every push.
      # The report still generates with placeholder data when bench results are absent.
      - name: Set up benchmark environment
        if: github.event_name != 'push'
        run: bash bench/setup.sh

      - name: Run benchmark (synth target, 3 runs)
        if: github.event_name != 'push'
        run: bash bench/compare.sh synth --runs 3

      # Vendor tests: run irradiate on real open-source Python libraries.
      # bench/setup.sh (run above) already bootstraps corpora and sets up their venvs.
      # Skipped on push to keep CI fast; report shows placeholders when file is absent.
      - name: Run vendor tests
        if: github.event_name != 'push'
        run: bash scripts/run_vendor_tests.sh -o vendor_results.json

      # Generate the report (gracefully handles missing benchmark and vendor data)
      - name: Generate report
        run: uv run --python 3.12 scripts/generate_report.py -o report/index.html --vendor-results vendor_results.json

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: report

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
